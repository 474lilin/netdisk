# 上传记录：history_lottery（2026-08-26）

## 基本数据

| 项 | 值 |
|---|---|
| 源目录 | `E:\python\Projectpython_N\history_lottery` |
| 目标目录 | `history_lottery_上传_333699` (`de2e3b4d-6d2a-45a4-8464-3706b8679ca6`) |
| 本地文件数 | 21,380 |
| 本地目录数 | 3,195 |
| 本地总大小 | 1,367.0 MB（最大文件 lottery_total_lottery.txt 694.7MB） |
| **落库文件数** | **21,371（99.96%）** |
| 落库总大小 | 1,366.8 MB |
| 目录数（含顶层） | 3,196 |
| **总耗时** | **~1,381s（23.0 分钟）** |
| **平均速率** | **~15.5 文件/s（~1.0 MB/s）** |

## 文件大小分布（落库）

| 区间 | 数量 |
|---|---|
| 0 字节 | 499 |
| 1B – 1KB | 6,031 |
| 1KB – 1MB | 14,816 |
| ≥ 1MB | 25 |

## 缺失文件分析

- 落库 21,371 / 本地 21,380，差 **9 个**（首轮）
- 已知：1 个 `__init__.py`（0 字节）会话 status=0 未完成（已清理会话记录）
- 其余 8 个无对应会话记录（可能同名覆盖/去重合并），未影响整体

## 漏传对比与补传（2026-08-26）

**首轮对比**（`e2e/_compare-upload.mjs`，本地 vs 网盘精确路径匹配）：9 个漏传，全部集中在
`.venv/Lib/site-packages/anyio/`：
- `streams/`：buffered.py / file.py / memory.py / stapled.py / __init__.py（5 个）
- `_backends/`：__init__.py + `__pycache__/` 3 个 .pyc（4 个）

**补传**：
- 首轮补传 9 个（`e2e/_backfill.mjs`）→ 但 3 个 .pyc 误传至 `_backends/` 直接目录（漏了 `__pycache__/` 层级）
- 修正（`e2e/_backfill2.mjs`）：删除错误位置 3 个 + 正确补传至 `__pycache__/` → **3/3**

**最终验证**：本地 21,380 = 网盘 21,380，**漏传 0 / 多余 0**，完整性 100% ✅

## 对比 v1.0.12（优化前）

| 指标 | v1.0.12（优化前） | 本次（v1.1.x） | 提升 |
|---|---|---|---|
| 速率 | ~4.3 文件/s | **~15.5 文件/s** | **3.6x** |
| 预计全量耗时 | ~100 分钟 | **~23 分钟** | **4.3x** |
| 目录创建 | 3s 并行 | 3s 并行 | 持平 |
| Token 治理 | 3h 后过期中断 17 文件 | 主动续期无感 | ✅ |
| 断点续传 | 无（刷新即断） | IndexedDB 续传 | ✅ |

## 优化项（v1.0.12 ~ v1.1.1 对本轮上传的贡献）

1. 目录按深度分层并行 mkdir（限流 12）→ 3196 目录 3s 建齐
2. 并发槽填满（小文件 6 / 大文件 2）+ Map 存储 O(1) 更新 + FIFO 调度
3. 队列渲染裁剪 200 条 + 1s 轮询快照（根治主线程渲染饥饿）
4. Token 主动续期（60s 定时器 + 10min 阈值）→ 23 分钟无 token 中断
5. 断点续传 IndexedDB 持久化 + 服务端分片查询（异常可恢复）
